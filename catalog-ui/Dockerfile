FROM node:18-alpine

WORKDIR /app

COPY . .

ARG ELASTICSEARCH_URL
ARG UI_BASE_PATH

ENV ELASTICSEARCH_URL $ELASTICSEARCH_URL
ENV UI_BASE_PATH $UI_BASE_PATH

RUN npm install
RUN npm run build

CMD ["npm", "run", "start"]